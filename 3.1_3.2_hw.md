# 3.1 GO & 3.2 KEGG_hw

**姓名：** 姚茗子  
**学号：** 2024030045

---

## 1）从wt.light.vs.dark.all.txt中选取显著上调的（FDR<0.05, logFC>1）的基因进行GO分析

**操作步骤：**

1. 打开差异表达结果文件 `wt.light.vs.dark.all.txt`，筛选满足条件（FDR<0.05, logFC>1）的基因。提取筛选后基因的 Ensembl Gene ID，保存为新的基因列表文件。

```bash
awk -F '\t' 'NR>1 && $3 > 1 && $7 < 0.05 {print $1}' wt.light.vs.dark.all.txt > upregulated_genes.txt
```

2. 前往 [Gene Ontology 官网](http://geneontology.org/)，将筛选出的基因 ID 粘贴到分析框中，选择物种 *Arabidopsis thaliana*。

3. 查看富集结果，部分结果如下图所示：

<img width="2370" height="1266" alt="image" src="https://github.com/user-attachments/assets/1ac6d939-6cf1-4020-b37a-288ef2e6ba7a" />
<img width="2357" height="1322" alt="image" src="https://github.com/user-attachments/assets/4f7f6623-4b15-44dc-9c08-9f5f5bbcc27b" />

这部分结果表明，野生型拟南芥在光照条件下上调的基因高度集中在**光信号感知**（如响应光刺激与UV-A）和**叶绿体发育**（如叶绿体伸长与组织）相关过程中。同时，植物大量激活了黄酮醇等次生代谢物合成及光修复基因来进行 **光保护**。这印证了植物见光后“开启光合工厂 + 启动防晒修复机制”的正常生理适应反应。

---

## 2）请问上面的例子中，Fold Enrichment和P value是如何计算的？请写出公式，并解释原理。此外，在定义显著富集的GO terms时为什么一般不是参考P value的大小，而是要计算一个FDR来作为参考？

### Fold Enrichment 的计算

**公式：**

$$\text{Fold Enrichment} = \frac{k / n}{K / N}= \frac{k}{n} \div \frac{K}{N} = \frac{k \cdot N}{n \cdot K}$$

其中：
- $N$：参考基因组中所有被 GO 注释的基因总数
- $n$：用户上传基因中成功映射到背景的基因数
- $K$：参考基因组中被注释到某一 GO term 的基因数
- $k$：用户上传基因中被注释到该 GO term 的基因数

**原理：** Fold Enrichment 衡量的是"实际观测到的富集程度"相对于"随机期望值"的倍数。若 Fold Enrichment > 1，说明该 GO term 在用户基因集中比随机情况下更多地出现，即存在富集；倍数越大，富集程度越高。

### P value 的计算

**使用超几何分布（Hypergeometric Test）：**

$$P = \sum_{i=k}^{\min(n,K)} \frac{\binom{K}{i} \binom{N-K}{n-i}}{\binom{N}{n}}$$

**原理：** 超几何分布描述的是"不放回抽样"问题。将参考基因组中的 $N$ 个基因看作总体，其中 $K$ 个属于某 GO term；从中随机抽取 $n$ 个基因（即用户上传的基因集），问恰好有 $k$ 个或更多属于该 GO term 的概率是多少。

- 若该概率（P value）很小，说明用户基因集中出现如此多该 GO term 基因并非偶然，即该 GO term 在用户基因集中显著富集。
- P value 越小，富集越显著。

### 为什么要用 FDR 而不是直接用 P value？

**多重检验问题（Multiple Testing Problem）：**

在 GO 富集分析中，我们同时对**数千个 GO terms** 进行统计检验。即使每个检验的显著性阈值设为 $\alpha = 0.05$，在没有任何真实富集的情况下，平均也会有 $5\%$ 的 GO terms 被错误地判定为显著（即假阳性）。

例如，若同时检验 1000 个 GO terms，即使所有 GO terms 都不富集，也预期会有约 $1000 \times 0.05 = 50$ 个 GO terms 的 P value < 0.05，这些都是假阳性结果。

**FDR（False Discovery Rate，假发现率）的意义：**

FDR 是对多重检验进行校正的方法，常用 **Benjamini-Hochberg（BH）方法** 计算。FDR 控制的是"在所有被判定为显著的结果中，假阳性所占的比例"。

- 使用 FDR < 0.05 作为阈值，意味着在所有被认为显著富集的 GO terms 中，最多允许 5% 是假阳性
- 相比直接使用 P value，FDR 更加严格，能有效控制假阳性率，使结果更可靠

**结论：** 在进行大规模多重检验时（如 GO 富集分析），必须使用 FDR 而非原始 P value 作为显著性判断标准，以避免大量假阳性结果干扰生物学解读。

---

## 3）请用KEGG enrichment分析上一章（GO enrichment analysis）中的基因集，比较两章的结果，总结两种方法得到的生物学意义有哪些异同

使用与上章作业 GO 分析相同的基因集（`upregulated_genes.txt`），在 [DAVID](https://david.ncifcrf.gov/) 平台上进行 KEGG 富集分析，结果如下：

<img width="2638" height="1417" alt="image" src="https://github.com/user-attachments/assets/d3ca9f11-cea5-4656-9c85-b0bf9ce44edd" />

### GO 与 KEGG 富集分析结果的比较

#### 1. 相同点
* **GO 分析**中，发现了显著富集的 `flavonol biosynthetic process`（黄酮醇生物合成）和 `terpenoid biosynthetic process`（萜类生物合成）。
* **KEGG 分析**中，同样发现了高度显著的 `Biosynthesis of secondary metabolites`（次生代谢物合成）、`Flavonoid biosynthesis`（黄酮类生物合成）以及 `Sesquiterpenoid and triterpenoid biosynthesis`（倍半萜和三萜类生物合成）。
* **生物学意义一致性**：这说明无论用哪种数据库，都能发现野生型拟南芥在见光后，会大量启动次生代谢途径，合成抗氧化和吸收紫外线的物质来进行光保护。

#### 2. 不同点（观察数据的视角不同）
* **GO 分析揭示基因的功能类别，更偏向于描述基因的功能属性与细胞器发育（如对光响应、叶绿体发育）**：
    * GO 抓取到了像“对光/UV-A的响应”、“叶绿体伸长与组织”这样的条目，给基因贴明确功能标签，表明这些基因正在对外界刺激（光）做出反应，并且在组装细胞器（叶绿体）。这在 KEGG 现有的代谢通路图里很难直观体现。
* **KEGG 分析揭示基因参与的具体通路，更直观地揭示了基因参与的系统级生理网络（如显著富集的“植物昼夜节律”通路）及具体的代谢流水线**：
    * KEGG 发现通路：**`Circadian rhythm - plant`（植物昼夜节律）**，且极其显著（Benjamini = 9.18e-7），光照是植物调节生物钟最重要的信号（Zeitgeber）。KEGG 直接把这些基因归类到了“调整生物钟”这个具体的系统机制里。
    * 同时，KEGG 把基因放进了一张张相互联系的代谢网络图（Pathway）里，比如宏观的 `Metabolic pathways`，它更侧重于基因产物在化学反应链条上的上下游关系。

### 生物学意义总结

- **GO 分析**侧重于描述基因集的**功能属性**，能从分子功能、生物过程、细胞定位三个角度全面刻画基因集的特征，适合初步了解基因集的整体功能倾向。
- **KEGG 分析**侧重于揭示基因集参与的**具体生物学通路**，结果更具机制性和可解释性。

- **两者互补**：GO 分析提供功能分类的全景视图，KEGG 分析提供通路层面的机制解析。在实际研究中，通常同时进行两种分析，以获得更全面的生物学解读。对于同一基因集，GO 和 KEGG 分析结果往往在生物学主题上一致（如都指向细胞增殖相关功能），但 KEGG 能提供更具体的通路信息，而 GO 能提供更丰富的功能层次描述。
